# Issue #200: CI: Add agent/* branch triggers and workflow polish

## Tasks

### Completed

- [x] Implement as described in the issue — added `agent/*` and `aloop/*` branch triggers, concurrency control, `type-check` job, `cli-tests` job with build step, dashboard npm cache, and removed invalid README `opencode run --agent` docs. All QA and review gates passed.
