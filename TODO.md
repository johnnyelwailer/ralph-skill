# Issue #23: Epic: Inner Loop Engine — Phase Control, Retry & Finalizer

## Tasks

- [ ] Implement as described in the issue
- [ ] [qa/P1] loop_finalizer_qa_coverage.tests.sh fails: `check_finalizer_qa_coverage_gate` and `append_plan_task_if_missing` functions referenced by test file do not exist in loop.sh — all 4 tests fail with `command not found`. Run `bash aloop/bin/loop_finalizer_qa_coverage.tests.sh` to reproduce. (priority: high)
- [ ] [qa/P1] Branch sync not implemented in loop.sh: spec requires pre-iteration `git fetch origin <base_branch>` + merge + conflict queueing (`PROMPT_merge.md`) + `merge_conflict` log event, but no such code exists in loop.sh. Grep `git fetch` or `merge_conflict` in loop.sh returns empty. (priority: high)
- [ ] [review] Gate 5/Constitution Rule 1: `npm run type-check` fails with 8 TypeScript errors caused by out-of-scope changes merged into this branch. `launchChildLoop` in orchestrate.ts (line 3166) references `state` not in its parameter list; line 3195 references `roundRobinOrder` also undefined; lines 5166/5178 reference `provider` undefined. `OrchestratorState` type has no `round_robin_order` property (line 3483). `process-requests.test.ts` imports 6 symbols never exported: `formatReviewCommentHistory`, `getDirectorySizeBytes`, `pruneLargeV8CacheDir`, `syncMasterToTrunk`, `syncChildBranches`, `ChildBranchSyncDeps`. Fix: revert all out-of-scope changes to `orchestrate.ts` and `process-requests.ts` to master state (or fix the type errors directly). (priority: critical)
- [ ] [review] Gate 5: `npm test` reports 33 failures (1077 pass). Primary failures: all 15 `launchChildLoop` subtests fail (orchestrate.test.ts test 355) due to the broken `state`/`roundRobinOrder` scope issue above; `dispatchChildLoops` tests 1,2,3,6,7,8,9 fail (test 356); `runOrchestratorScanPass` tests 7-8 fail (test 376); `processQueuedPrompts` (test 391); `process-requests.test.ts` tests 9-10 (test 7); `EtagCache` (test 594). Fix the type errors from the [review] task above and rerun. (priority: critical)
- [ ] [review] Constitution Rule 1 VIOLATION: `loop.sh` is 2373 LOC. Constitution Rule 1 is a hard rule: "Nothing may be added to loop.sh or loop.ps1. Any PR that touches these files must reduce their line count." The commits since the last review (777a4fba: +36 lines, 32f56f0d: +5, 2bdd235c: +27) added `extract_explicit_cooldown_until`, stale-lock recovery in `acquire_provider_health_lock`, and `tmp_stdout` capture. These additions violate the constitution. Remove or relocate these additions to the CLI (or justify via spec requirement and reduce LOC elsewhere by at least the same amount). (priority: critical)
- [ ] [review] Constitution Rules 2, 12, 18 — out-of-scope changes: `orchestrate.ts` and `process-requests.ts` are explicitly out of scope per SPEC.md. The following changes were made to these files and must be justified or reverted: (a) orchestrate.ts — validateDoR criterion 1 regex change, criterion 5 removal, `dor_validated` guard in `getDispatchableIssues`, `round_robin_order` state passthrough in `launchChildLoop`, SPEC.md seeding in worktree, hardcoded iteration limit removals (793ba420, d45e7abd). (b) process-requests.ts — `sweepStaleRunningIssueStatuses` addition (4472e66b merge). If these changes belong to other issues/PRs they should be isolated there, not bundled here. (priority: high)
