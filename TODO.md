# TODO

- [x] [p0] Rename repository paths and in-repo runtime/command/skill/prompt references from legacy name to `aloop` (Phase 0 rename baseline).
- [ ] [p0] Implement Node.js CLI entrypoint `aloop/cli/aloop.mjs` with `resolve` command and shared config/project helpers.
- [ ] [p1] Replace prompt/command duplicated runtime-root resolution with `aloop resolve`.

