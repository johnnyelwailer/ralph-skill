# Issue #94: Pipeline must be 100% data/config driven — no hardcoded intervals or thresholds

## Tasks

- [x] Implement as described in the issue
- [ ] [qa/P1] Fix hot-reload: `aloop start` must write `loop_settings` to meta.json — In `start.ts`, after `compileLoopPlan()` writes loop-plan.json to `sessionDir`, call `loadLoopSettings(path.join(sessionDir, 'loop-plan.json'))` and add the result as `loop_settings` to the `meta` object before writing meta.json. Also add `loop_settings` to the `SessionMeta` interface. Add a test in `start.test.ts` that verifies meta.json contains `loop_settings` with the correct pipeline.yml values. (priority: high)
- [ ] [review] Gate 4: `loop.sh` and `loop.ps1` re-introduced dead orchestrator-level variables — `TRIAGE_INTERVAL`, `SCAN_PASS_THROTTLE_MS`, `CONCURRENCY_CAP`, `RATE_LIMIT_BACKOFF`, `COST_PER_ITERATION_USD`, `BUDGET_APPROACHING_THRESHOLD` are initialized and loaded from config (loop.sh:313-315,323,348-354; loop.ps1:108-119, Load-LoopSettings, Refresh-LoopSettingsFromMeta) but **never referenced in any actual loop logic**. These are orchestrator-level settings (used only in `orchestrate.ts`). They were previously removed as dead code in commit `66abdb0`. Commits `450795d9a` and `08d6aea79` re-introduced them. Remove from both files. Architecture rule: orchestrator-level settings belong only in `orchestrate.ts`. (priority: high)
- [ ] [review] Gate 3: `compile-loop-plan.ts` added 18 new fields to `LoopSettings` and `numFields` (retry_backoff_linear_step_secs, retry_backoff_exponential_base, phase_retries_min, cost_per_iteration_usd, budget_approaching_threshold, qa_coverage_gate_max_untested_pct, concurrency_cap, git_fetch_timeout_ms, git_merge_base_timeout_ms, gh_cli_timeout_ms, gh_watch_interval_secs, gh_watch_max_concurrent, gh_feedback_max_iterations, gh_ci_failure_persistence_limit, gh_etag_cache_ttl_ms, priority_critical, priority_high, priority_low) — none are tested in `compile-loop-plan.test.ts:847`. Add all 18 to the pipeline.yml fixture and add exact `assert.equal(loopPlanJson.loopSettings.<field>, <expected_value>)` assertions for each. (priority: high)
- [ ] [review] Gate 3: `aloop/cli/src/lib/loop-settings.ts` is a new 180-line module with zero test coverage. Create `loop-settings.test.ts` covering: (1) loadLoopSettings with a valid loop-plan.json — assert all fields match exact values; (2) loadLoopSettings with missing/unreadable file — assert all fields fall back to DEFAULT_LOOP_SETTINGS values; (3) readNumberArray inline-array-string parsing (e.g. "[0,120,300]"); (4) readStringEnum rejects invalid value and uses fallback; (5) loadLoopSettings returns a frozen object. (priority: high)
