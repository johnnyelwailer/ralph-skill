{
  "name": "ralph-skill",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun --filter '*' typecheck",
    "build": "bun --filter '*' build",
    "check:constitution": "bun run tools/check-constitution-loc.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.5",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "istanbul": "^0.4.5"
  }
}
